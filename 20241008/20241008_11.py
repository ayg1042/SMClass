








if __name__=="__main__":

  ss = '파이썬 수업중 타입 문자열 타입, 정수형 타입, 실수형 타입, 논리형 타입이 있습니다.'
  print(ss.replace(' ', ''))
  print(ss)


  # i_str = input('글자를 입력하세요.')
  # # i_str = '타입'
  # idx = 0
  # a_list = []
  # for i in range(ss.count(i_str)):
  #   num = ss.find(i_str, idx)
  #   a_list.append(num)
  #   idx = num + 1

  # print(f'검색 개수 : {len(a_list)} 위치값 : ',a_list)

  # # 1 ~ 20 중에 3의 배수만 리스트에 추가하시오.
  # a_list = [i for i in range(3, 21, 3)]
  # b_list = [i for i in range(1, 21) if i % 3 == 0]
  # print(a_list)
  # print(b_list)

  # a_list = []
  # for i in range(1, 21):
  #   if i % 3 == 0: a_list.append(i)
  # print(a_list)

  # aArr = [1, 2, 3, 4, 5]
  # a_list = [i * i for i in aArr]
  # # a_list = [1, 4, 9, 15, 25] 한줄 for문으로